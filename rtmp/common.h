#ifndef __COMMON_H
#define __COMMON_H

#include <windows.h>
#include <stdint.h>


#define RTMP_MIN(a,b)	(((a) < (b)) ? (a) : (b))


#define RTMP_DEFAULT_PORT	1935

#define IN  
#define OUT

#define RTMP_PACKET_MAX_LENGTH 1024


#endif